#include <stdio.h>

int main() {
    int n = 5;  // height of half diamond
    int i, j;

    // Upper half including middle
    for(i = 1; i <= n; i++) {
        for(j = 1; j <= (2*i - 1); j++) {
            printf("*");
        }
        printf("\n");
    }

    // Lower half
    for(i = n-1; i >= 1; i--) {
        for(j = 1; j <= (2*i - 1); j++) {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
