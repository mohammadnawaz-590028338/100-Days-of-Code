#include <stdio.h>
int main() {
    int i, j;
    for(i = 1; i <= 5; i++) {              // rows
        for(j = 5 - i + 1; j <= 5; j++) {  // print from (5-i+1) to 5
            printf("%d", j);
        }
        printf("\n"); // move to next line
    }
    return 0;
}
