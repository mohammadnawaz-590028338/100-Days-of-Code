#include <stdio.h>

int main() {
    int n, i;

    // Input the value of n
    printf("Enter a number: ");
    scanf("%d", &n);

    // Using a for loop to print numbers from 1 to n
    for(i = 1; i <= n; i++) {
        printf("%d ", i);
    }

    printf("\n");
    return 0;
}
✅ Explanation:

We take the number n from the user.

A for loop starts from 1 and goes up to n.

Each number is printed followed by a space.

You could also do this with a while loop:

c
Copy code
#include <stdio.h>

int main() {
    int n, i = 1;

    printf("Enter a number: ");
    scanf("%d", &n);

    while(i <= n) {
        printf("%d ", i);
        i++;
    }

    printf("\n");
    return 0;
}
