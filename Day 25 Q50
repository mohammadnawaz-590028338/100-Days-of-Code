#include <stdio.h>

int main() {
    int i, j;
    for(i = 5; i >= 1; i--) {        // outer loop for rows
        for(j = 1; j <= i; j++) {    // inner loop for stars
            printf("*");
        }
        printf("\n");  // move to next line
    }
    return 0;
}
